<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.car_info_backend.car.mapper.CarMapper">
    <resultMap id="car" type="com.green.car_info_backend.car.dto.CarDTO">
        <id     column="MODEL_NUM" property="modelNum"/>
        <result column="MODEL_NAME" property="modelName"/>
        <result column="MODEL_PRICE" property="modelPrice"/>
        <result column="MADE_COMPANY" property="madeCompany"/>
    </resultMap>

    <!--차량관리화면(차량목록조회)-->
    <select id="getCarList" resultMap="car">
        SELECT MODEL_NUM
            , MODEL_NAME
            , MODEL_PRICE
            , MADE_COMPANY
        FROM car_info
    </select>

    <!--차량관리화면(차량등록)-->
    <insert id="insertCar">
        INSERT INTO CAR_INFO (
          MADE_COMPANY
        , MODEL_NAME
        , MODEL_PRICE
        ) VALUES (
          #{madeCompany}
        , #{modelName}
        , #{modelPrice}
        )
    </insert>


	
</mapper>



